import setuptools

setuptools.setup(
    name="utils",
    version="0.0.1",
    description="Helper utilities to deal with common tasks in ML",
    packages=setuptools.find_packages(),
    python_requires='>=3.10',
)